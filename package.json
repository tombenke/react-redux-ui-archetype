{
  "name": "{{package_name}}",
  "description": "{{package_description}}",
  "version": "1.0.0",
  "tags": [
    "web",
    "ui",
    "react",
    "redux"
  ],
  "author": {
    "name": "{{author_full_name}}",
    "email": "{{author_email}}"
  },
  "bugs": {
    "url": "https://github.com/{{author_github_account}}/{{package_name}}/issues"
  },
  "license": "MIT",
  "homepage": "https://github.com/{{author_github_account}}/{{package_name}}",
  "repository": {
    "type": "git",
    "url": "http://github.com/{{author_github_account}}/{{package_name}}.git"
  },
  "engines": {
    "node": "^6.11.2"
  },
  "bin": {},
  "main": "index.js",
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-redux": "^5.0.6",
    "monitoring-ui-store": "git+https://github.com/tombenke/monitoring-ui-store.git"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.3.13",
    "babel-preset-react": "^6.3.13",
    "babelify": "^7.2.0",
    "browserify": "^12.0.1",
    "pre-push": "^0.1.1",
    "eslint": "^3.10.2",
    "eslint-plugin-react": "^7.5.1",
    "reactify": "^1.1.1",
    "uglify-js": "^2.6.1",
    "watchify": "^3.6.1"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015",
            "react"
          ]
        }
      ]
    ]
  },
  "scripts": {
    "lint": "eslint src/**.js",
    "test": "cross-env NODE_ENV=test mocha --recursive 'src/**/*.spec.js'",
    "watch": "watchify src/app.js -o dist/js/bundle.js -v",
    "browserify": "browserify src/app.js | uglifyjs > dist/js/bundle.js",
    "build-js": "browserify -t [babelify] src/app.jsx > dist/js/bundle.js",
    "build": "npm run browserify;"
  },
  "pre-push": [
  ]
}
